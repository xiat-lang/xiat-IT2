-- 003 : basic parser view --

the parser (IT2) is a vry simple one, consiting of 1 general node with following:
type
value
children (list of nodes)

where adds new nodes into a child of another node if stacked by blocks
(see BLOCKOPEN token and BLOCKCLOSE token in 002)

this page is one of smallest ones since there isnt much, but you can go back here for a reference


(002 - basic lexer view) < . > (004 - branches and varibles)
